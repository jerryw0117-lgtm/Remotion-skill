# Public Assets Required

Original source media is intentionally not included in this share package.

Create this folder:

```text
public/
```

Then add your own media with these filenames:

```text
public/source.mp4
public/music.mp3
```

After adding assets:

```bash
npm install
npm run dev
```
